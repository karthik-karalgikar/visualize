instructions to run

cd backend 
python app.py

split terminal
cd frontend
npm run dev

## Requirements
- Python >= 3.9

## Setup
```bash
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
